package com.codecool.core.buffer;

/**
 * It is a util like class to support Buffer.
 * The enums represents the different actions
 * that Buffer can do in the
 * Buffer.process() method.
 * */

public enum BufferTag {
    ADD, REMOVE
}
